import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core';
export const subjects = sqliteTable('subjects', { id:text('id').primaryKey(), owner:text('owner').notNull(), name:text('name').notNull(), color:text('color').notNull(), created:text('created').notNull() }, t=>[index('idx_subjects_owner').on(t.owner)]);
export const notes = sqliteTable('notes', { id:text('id').primaryKey(), owner:text('owner').notNull(), subject:text('subject').notNull().references(()=>subjects.id,{onDelete:'cascade'}), title:text('title').notNull(), body:text('body').notNull(), updated:text('updated').notNull(), summary:text('summary').notNull().default(''), blocks:text('blocks').notNull().default('') },t=>[index('idx_notes_owner_subject').on(t.owner,t.subject)]);
export const files = sqliteTable('files', { id:text('id').primaryKey(), owner:text('owner').notNull(), note:text('note').notNull().references(()=>notes.id,{onDelete:'cascade'}), name:text('name').notNull(), size:integer('size').notNull(), key:text('key').notNull() },t=>[index('idx_files_owner_note').on(t.owner,t.note)]);

export const exams = sqliteTable('exams', { id:text('id').primaryKey(), owner:text('owner').notNull(), subject:text('subject').notNull(), title:text('title').notNull(), date:text('date').notNull(), time:text('time'), content:text('content'), created:text('created').notNull() }, t=>[index('idx_exams_owner_date').on(t.owner,t.date)]);
